import { createApp } from "vue";
import App from "./App.vue";

// T1.1 placeholder — T3 will wire vue-router + pinia-less composables here.
// For now just mount the placeholder App so vite build pipeline is verifiable.
const app = createApp(App);
app.mount("#app");
